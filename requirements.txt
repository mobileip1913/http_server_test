websockets>=12.0
aiohttp>=3.9.0
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.2
opuslib>=2.0.0  # Opus 音频编解码（用于分割连续的 Opus 数据包）

# Web服务依赖
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
reportlab>=4.0.0  # PDF生成

# 可选依赖（用于音频处理）
# gtts>=2.3.0  # Google Text-to-Speech（如果使用 TTS）
# pydub>=0.25.1  # 音频处理库（如果使用）

