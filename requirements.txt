# WebSocket 客户端依赖
websockets>=12.0  # WebSocket 客户端（用于连接服务器）

# Opus 音频编解码
opuslib>=2.0.0  # Opus 音频编解码（需要系统级别的 Opus 库，见 README_DEPENDENCIES.md）

# Web服务依赖
flask>=3.0.0  # Flask Web框架
flask-socketio>=5.3.0  # WebSocket支持
flask-cors>=4.0.0  # CORS支持
reportlab>=4.0.0  # PDF报告生成

# 注意：以下包在当前代码中未使用，但保留以防将来需要
# aiohttp>=3.9.0  # 异步HTTP客户端（未使用）
# pandas>=2.0.0  # 数据分析库（未使用）
# numpy>=1.24.0  # 数值计算库（未使用）
# python-dateutil>=2.8.2  # 日期处理库（未使用）

